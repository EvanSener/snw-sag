# OpenSpec Main Specifications

本目录用于保存当前项目行为的长期主规范。

约定：

1. 每个 capability 使用一个独立目录，例如 `specs/user-auth/spec.md`。
2. 新变更默认先在 `openspec/changes/<change-name>/specs/` 中产出 delta specs。
3. 只有当能力行为成为当前系统的新基线时，才将其同步回本目录。

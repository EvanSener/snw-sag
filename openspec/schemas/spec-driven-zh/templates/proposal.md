## 为什么

<!-- 说明本次变更的动机。它解决什么问题？为什么现在做？ -->

## 变更内容

<!-- 描述会发生什么变化。明确写出新增能力、修改项或移除项。 -->

## 能力范围

### 新增能力
<!-- 本次新增的 capability。将 <name> 替换为 kebab-case 标识符（例如 user-auth、data-export、api-rate-limiting）。每项都会对应一个 specs/<name>/spec.md -->
- `<name>`: <简要说明该 capability 覆盖的行为范围>

### 修改能力
<!-- 现有 capability 中 REQUIREMENTS 发生变化的项（不是纯实现细节）。
     只有 spec 层行为变化时才写在这里；每项都需要对应 delta spec。
     使用 openspec/specs/ 中已有的 spec 名称。如果没有 requirement 变化，可以写 None。 -->
- `<existing-name>`: <本次变更修改了什么 requirement>

## 影响范围

<!-- 受影响的代码、API、依赖、模块或系统 -->

# OpenSpec Changes

本目录用于保存单次变更的 proposal、specs、design 和 tasks。

约定：

1. 变更目录默认命名为 `chg-YYYY-MM-DD-<change-name>`。
2. 每个变更目录应至少包含：
   - `proposal.md`
   - `design.md`
   - `tasks.md`
   - `specs/`（如果存在 capability 变更）
3. 完成后的变更可归档到 `changes/archive/`。
